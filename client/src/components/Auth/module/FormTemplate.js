import React from 'react';

const FormTemplate = ({ type, inputFor, style, handlerType, handler }) => (
  <>
    <label htmlFor={inputFor}></label>
    <input type={}/>
  </>
);

export default FormTemplate;